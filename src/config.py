"""Central path configuration for the pipeline.

All paths are relative to the repository root. Scripts are expected to be run
with the repo root as the working directory, e.g. `python src/main.py` from
the top of the repo.
"""

DATA = "data"
NN_CACHE = "nn_cache"
SUB = "sub"
CONFIGS = "configs"
